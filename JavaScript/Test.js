function romanToInt(s) {
    var arr = s.split("");
    var symbols = ["I", "V", "X", "L", "C", "D", "M"];

    // if (arr.indexOf(symbols) != -1){
    //     console.log("match");
    // }
    console.log(arr.indexOf(symbols));
}

romanToInt("III");